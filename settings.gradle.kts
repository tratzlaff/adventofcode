// Always add a settings.gradle.kts to the root directory of your build to avoid the initial performance impact.
// The file can either be empty or define the desired name of the project.
// A multi-project build must have a settings.gradle(.kts) file in the root project of the multi-project hierarchy.
// It is required because the settings file defines which projects are taking part in a multi-project build.

rootProject.name = "adventofcode"

